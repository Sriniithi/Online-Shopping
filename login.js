// Function to handle customer login and redirect to home page
async function customerLogin(email, password) {
    // Add authentication logic here (using Firebase Auth or a custom solution)
    
    // On successful login
    window.location.href = "home.html"; // Redirect to home page where products are listed
}
